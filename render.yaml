services:
  - type: web
    name: journal-platform
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn journal_project.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: journal_project.settings_production
      - key: PYTHON_VERSION
        value: 3.10.12
